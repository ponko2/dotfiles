#!/usr/bin/env bash

set -euo pipefail

# mise
mise self-update --yes
mise bootstrap --yes --update -C ~/.dotfiles
mise bootstrap packages upgrade --yes -C ~/.dotfiles
mise upgrade --yes -C ~/.dotfiles
mise prune --yes

# Homebrew
brew update --auto-update
brew upgrade --fetch-HEAD

# Yazi
ya pkg upgrade

# Zsh
sheldon lock --update
rm -rf "${XDG_CACHE_HOME:-$HOME/.cache}/zsh" && mkdir -p "$_"
zsh -c 'autoload -Uz compinit; compinit -d "$XDG_CACHE_HOME/zsh/zcompdump-$ZSH_VERSION"'

# Neovim
nvim --headless "+Lazy! update" +qa
